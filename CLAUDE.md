# JOJO Director

职教微课 AI 创作画布：脚本→分镜→帧→视频→质检→拼接的节点式产线。
后端 FastAPI + SQLite（backend/），前端 React + Vite（frontend/），模型走火山方舟（providers.yaml 路由）。

## 约定

- 系统提示词全部外置在 `backend/prompts/*.md`（mtime 热加载，改完即生效，不用重启）
- 质检/保真/打法规则在 `backend/qc_rules/*.yaml`，自然语言可直接编辑；判卷注意力有限，单次组卷规则总量要克制
- 新增节点类型必须四处同步登记：executors 执行器注册、auth.py 限额表、前端 TYPE_META 表单、prompts/agent_system.md 契约行——缺一处就是幽灵功能
- 部署=本地构建+文件直推+重启+健康检查（服务器到 GitHub 的网络不可靠，禁止在服务器上 git pull）
- 长任务（视频/切分/增强/拼接/配音）一律后台执行+轮询取结果，HTTP 接口只受理

## Known issues（按优先级分级；教训在决策日志中出现满 3 次即晋升到此处）

### P0 — 每次生产都可能翻车

- **提及即召唤**：图像提示词里否定句的实物名词照样被画出来（"no laser"反而召唤光束）；领域术语会召唤示意图（"interferometer"召唤坐标图和标注）。只写想要的画面，用白话物体描述替代术语。分镜 lint 会对"no/不要+实物名词"告警
- **HTTP 409 = 已受理，转轮询**：网关掐断长请求后重发 execute 会收到 409"节点正在执行中"——这不是失败，误判会导致错误降级（曾错剥尾帧）
- **节点失败只 merge 不覆盖 outputs**：整体覆盖会抹掉用户上传素材和历史成果
- **Seedance mini 图生视频上限 720p**：1080p 仅纯文生可用；探针验证必须按真实调用模式（图生带图测），跨模式结论无效。拼接端负责上采样 1080p
- **重抽必须保留风格锚**：视觉裁判的修正建议若丢失 photorealistic 等风格词，会把写实镜头带偏成卡通（coach_system.md 已立铁律，改提示词时勿删）

### P1 — 高频运维坑

- **配音先行**：中文自然语速约 5 字/秒，解说字数上限=片长×5；语速微调只消化 ≤10% 余量。配音与字幕必须同取 script.narration（同文原则）
- **SQLite 热库禁止直接拷贝**：必迁移/备份就用 backup API 出快照（直拷必损坏）
- **进程树整杀**：Windows 重启后端用 taskkill //T，否则孤儿进程占端口跑旧代码；"改动后行为完全不变"先怀疑代码没加载
- **Bash heredoc 写中文/转义序列会损坏文件**：改代码用编辑工具或 chr() 构造
- **编辑式生成三原则**：单一变化、纯末态、对象具体；禁 remove 类词（提及即召唤的编辑版）

### P2 — 已有防护但需知晓

- Seedance 首帧含照片级真实人脸会被平台拒；手部/肢体特写可用（GEN-04 只审人脸）
- 方舟素材 URL 24 小时过期，生成成功必须立即落盘（已实现）
- Chrome 自动翻译会破坏 React 画布（已加 notranslate，勿手动翻译页面）
- 成片交付前按等距 8 点抽帧过目全片——节点级质检拦不住跨镜头风格漂移

## 方法论

产线三定律（时间/空间/协作三类故障源）见 docs/methodology/：LOOP 执行循环律、HARNESS 环境保障律、GRAPH 数据契约律。
